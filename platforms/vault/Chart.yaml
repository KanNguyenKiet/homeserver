apiVersion: v2
name: vault-platform
description: HashiCorp Vault platform chart for the homeserver cluster
type: application
version: 0.1.0
appVersion: "2.0.3"
dependencies:
  - name: vault
    version: "0.34.0"
    repository: https://helm.releases.hashicorp.com
